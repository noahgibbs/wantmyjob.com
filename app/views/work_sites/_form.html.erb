<%= form_for(@work_site) do |f| %>
  <% if @work_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_site.errors.count, "error") %> prohibited this work_site from being saved:</h2>

      <ul>
      <% @work_site.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
