<% big_center_text "Admin Console" %>

<div class="spaced_container">
  <div class="approval infoblock">
    <h2> Stuff to Approve </h2>

    <p>
      Questions to Approve: <%= questions_to_approve.count %>
        <%= link_to("(Approve them)", :controller => :questions,
                    :action => :index) %> <br/>
    </p>
  </div>

  <div class="newstuff infoblock">
    <h2> New Stuff in the Last 24 Hours </h2>

    <p>
      New Users Today (<%= new_users.count %>):
        <%= new_users.map {|u| user_to_link(u)}.join(", ") %>  <br/>
      New Profiles Today (<%= new_profiles.count %>):
        <%= new_profiles.map {|p| profile_to_link(p)}.join(", ") %>  <br/>
      New Questions Today: <%= new_questions.count %> <br/>
      Total Answers: <%= Answer.count %> <br/>
      New Jobs Today (<%= new_jobs.count %>):
        <%= new_jobs.map {|j| job_to_link(j)}.join(", ") %> <br/>
    </p>
  </div>

  <hr />

  <%= form_tag(:action => :become_user) do %>
    <p> Log in as: </p>

    <%= select_tag("user_id",
                   options_from_collection_for_select(users, :id, :login)) %>
    <%= submit_tag "Change User" %>
  <% end %>

  <hr />

  <%= link_to("Stats feed", notes_path) %>
</div>
