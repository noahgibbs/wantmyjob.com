<div class="spaced_container">
  <h2>Resend unlock instructions</h2>

  <p> Did you miss the email about how to unlock your account, or did
    it get stuck in your spam filter? </p>

  <%= form_for(resource, :as => resource_name, :url => unlock_path(resource_name)) do |f| %>
    <%= devise_error_messages! %>

    <p><%= f.label :email %><br />
    <%= f.text_field :email %></p>

    <p><%= f.submit "Send them again, please" %></p>
  <% end %>

  <%= render :partial => "devise/shared/links" %>
</div>
